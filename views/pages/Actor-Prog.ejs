<%- include('../partials/header') %> 

<main class="main" id="main">

<section class="section">
    <div class="container">
        <div class="row item-center">
            <div class="col">
                <div class="card">
                    <div class="card-body">
                        <p><%= programs.fName %> <%= programs.lName %></p>
                        <p><%= programs.title %></p>
                        <p><%= programs.character %></p>
                        <p><%= programs.programs %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>             

</main>
<%- include('../partials/footer') %> 