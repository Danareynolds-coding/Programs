<%- include('../partials/header') %> 
<main class="main" id="main">
  <div class="container mt-4">
        <div class="row text-center">
            <div class="col">
                <div class="card">
                    <div class="card-body">
                        <p><%= data.genre %> 
                        <p><%= data.title %></p>
                        <p><%= data.yr_released %></p>
                        <p><%= data.rating %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

</main>

<%- include('../partials/footer') %> 